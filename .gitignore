# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Original bitmap exports from Figma. The WebP conversions in
# public/images/ are the versioned deliverable; these are the raw
# inputs to `npm run convert-images` and are kept locally only.
brand-source/images-original/

# Vite cache — the real one lives in node_modules/.vite; this root copy is an orphan
.vite
